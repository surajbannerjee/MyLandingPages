$(function () {

    if ($(".cmn-cards-main-rooms-design").length) {
        $(".cmn-cards-main-rooms-design").matchHeight();
    }

    if ($(".cards-furniture-offer-sec-inr").length) {
        $(".cards-furniture-offer-sec-inr").matchHeight();
    }

    if ($(".all-cmn-cards-discollection-inside").length) {
        $(".all-cmn-cards-discollection-inside").matchHeight();
    }

    if ($(".blog-inside-cards-cmn-all").length) {
        $(".blog-inside-cards-cmn-all").matchHeight();
    }
});